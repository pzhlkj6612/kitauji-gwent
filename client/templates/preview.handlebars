{{#if card.skinFaction}}
  <i class="card-{{this.size}}-{{card.skinFaction}} card-{{this.size}}-{{card.skinFaction}}__{{card.img}}"></i>
{{else}}
  <i class="card-{{this.size}}-{{card.faction}} card-{{this.size}}-{{card.faction}}__{{card.img}}"></i>
{{/if}}
<div class="preview-description {{#if previewB}}preview-b{{/if}}">
  <p>{{card.name}}</p>
  {{#each abilities as |val key|}}
    <p>{{val}}</p>
  {{/each}}
  {{#if hasAttackPower}}
  <p>{{i18n "attack_power"}}
    <span>{{attackPower}}</span>
  </p>
  {{/if}}
  {{#if hasGrade}}
  <p>{{i18n "grade"}}
    <span>{{grade}}</span>
  </p>
  {{/if}}
  {{#if hasRelatedCards}}
  <p>{{i18n "related_card"}}
    {{#each relatedCards as |val key|}}
    <span class="related-card {{#if val.owned}}owned{{/if}}">{{val.name}}&nbsp;</span>
    {{/each}}
  </p>
  {{/if}}
</div>