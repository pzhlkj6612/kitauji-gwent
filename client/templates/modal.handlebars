<div class="bbm-modal__topbar">
  <h3 class="bbm-modal__title">{{openDiscard.name}}{{i18n "discard_members"}}</h3>
</div>
<div class="bbm-modal__section small-cards-modal">
  {{#each openDiscard.discard as |val key|}}
    {{>card val}}
  {{/each}}
</div>
<div class="bbm-modal__bottombar">
  <span class="btn-sub bbm-close">{{i18n "close"}}</span>
</div>