{{#each this}}
  <div class="card{{#if _disabled}} disabled{{/if}}" data-key="{{_key}}" data-id="{{_id}}">
    {{#if diff}}
      <span class="card-attr-power {{#if diffPos}}card-attr-positive{{/if}}">
        <span>{{power}}</span>
      </span>
    {{/if}}
    <i class="card-md-{{_data.faction}} card-md-{{_data.faction}}-{{_data.img}}"></i>
  </div>
{{/each}}